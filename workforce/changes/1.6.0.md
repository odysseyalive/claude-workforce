# 1.6.0

*Released 2026-09-01. 1 commit(s), 16 file(s) changed. Surface: handbook, chart, principles, audit, verify, scripts. Reaches an existing org: YES.*

**An org built before this release carries a stale copy of something this release changed.** Updating the skill is not enough — run `/workforce audit` so the heal passes rewrite it.

---

dispatch fidelity: the ask survives the work (v1.6.0)

The first directive — the user's request verbiage is absolutely retained —
had authoring-time mechanics only. At dispatch time the work order was four
dispatcher-authored fields and none was the ask; returns were a =3-line
summary nothing read back. Measured on a real org: 252 spawn edges, mean
3.5 lines, 2% with exit criteria, 1 Lead-to-IC dispatch, and both prior
contract releases (v1.3.0, v1.5.0) template-only with zero deployed
handbooks carrying either.

- Work order carries five fields: REQUEST (verbatim) first, forwarded
  unreplaced down every hop; a missing block returns QUESTION (6c); the
  deliverable is read back against the REQUEST before reporting (6d).
- COMPLETENESS + PLAN-READINESS move into the dispatch CHECKPOINT (6e/6f)
  the main session actually reads; template copies kept.
- OUTPUT.md opens with the quoted REQUEST + criterion-to-evidence table
  (CEO and IC Reporting); Lead forwards byte-for-byte; CEO verification
  step (4) closes the loop against the request, not just the Objective.
- Spawn edges become fielded records (1a); review prints EDGE-FIELDS +
  DISPATCH-MIX; invariants rows 25-27: INV-ASK, INV-REMEDIATE (backfill —
  printed by Step 5c with no row), INV-REFRESH.
- Audit Step 5d: doctrine refresh — governed handbooks amended up to the
  installed contract in-run, re-probed, version-stamped (new ORG-RECORD
  workforce-version stamp; wf-conform advises, verify names staleness).
- Honesty: chart deny line is measured (NONE — prose-only when zero);
  org-doctrine retracted-Constitution-Gate claim corrected; wf-conform
  catches the allowed-tools dead-key ceiling; preflight prints the
  maintainer-mode receipt.
- 21 check assertions + 21 prove del-cases, all proven; repaired two
  carried VACUOUS (prove name-drift; session-split guard exemption scoped
  to the match vicinity after this batch's own edit disarmed it).

bin/check 1046/0 - bin/prove 350/350 provable - synced both installs.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01N3YKdKsgCckiNUfuThVk5G

---
