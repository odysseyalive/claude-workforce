# 1.35.2

*Released 2026-09-09. 2 commit(s), 6 file(s) changed. Surface: changes, installer. Reaches an existing org: no.*

---

Two cases that reported a verdict about neither the rule nor the mutation

The mutation sweep finished: 700 of 702 proven by breaking, working tree
untouched, baseline clean. Both non-proven verdicts were cases written earlier
today, and both were pointing at text their assertion never reads.

`hooks: the wired set, the manifest, and the authority table agree` deleted a
phrase from enforcement.md. That check reads SHIPPED_HOOKS, the manifest, and
hooks.md — never that phrase. It reported VACUOUS while three unrelated checks
failed, which is the worst shape a case can have: a mutation that breaks
something, attributed to a rule it did not touch. Retargeted at the mutation it
actually catches — removing a wired hook's row from the authority table — and
verified firing.

`prove: every case name is exactly the name of the check it breaks` had no
correct mutation at all, and the reason is worth recording rather than patching
around. Its failure mode is a SILENT PASS: break the blanket-pattern guard and
`_check_pats` swallows every orphan, so the check goes green having tested
nothing. No deletion can turn it red. A case that cannot make its check fire
proves nothing, so it is removed and the assertion joins the 162 the summary
already counts as structural — provable only by hand. That bucket exists so this
kind of assertion can be honest about itself instead of carrying a case that
reports a verdict about neither the rule nor the mutation.

Both were mine, written hours apart, and both were retargeted by NAME earlier
without checking the literal mattered to that assertion. Renaming a case is not
retargeting it.

check 1136 passed · 0 failed · 4 skipped. script-conformance 189/189.
prove 700/702 before this fix.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---

Regenerate the change record for 1.35.1

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---
