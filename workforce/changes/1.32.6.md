# 1.32.6

<!-- Enforcement (maintainer-facing; bin/ does not ship — on a host this is `/workforce verify`): 0 assertion(s) in bin/check name this file; 0 normative claims total. 8 generic assertions guard it too. Coverage is a floor, not a certificate. -->
*Released 2026-09-09. 2 commit(s), 7 file(s) changed. Surface: changes, installer. Reaches an existing org: no.*

---

A case that cannot run HERE is not a case to delete

Three prove cases name `.claude/` paths — the operating-principles skill and two
grounding files. They were reported STALE-CASE, whose message is "does not ship
— remove this case", and following that message would have deleted live coverage
for every project workforce is used on.

The user's correction is the fact that was missing: every workforce project
carries the org skill after its first audit. This repository is the one that does
not, because it is the DEVELOPMENT KIT — it builds the distribution rather than
being staffed by it. So those cases are correct, their coverage is real
everywhere the distribution is used, and they simply cannot run on this checkout.

They now report N/A with that reason and "Do NOT remove". STALE-CASE keeps its
meaning for what it was built for: a shipped file that no longer ships, where
removal IS the fix.

N/A leaves the ratio rather than counting as unproven. Counting it as a failure
would leave this suite permanently short of green on the one repository it lives
in, and a standing red teaches a reader to ignore the number — which is how the
457 unproven assertions stayed invisible in the first place. Counting it as
proven would be a lie about coverage. It gets its own line.

bin/check's four skip messages carried the same too-narrow framing — "this repo
carries no org and does not need one" — which reads as though an org were
optional in general. They now say what is true: the rule is real, it is measured
on every audited project, and this one checkout cannot measure it.

check 1128 passed · 0 failed · 4 skipped.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---

Regenerate the change record for 1.32.5

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---
