# 1.32.2

*Released 2026-09-09. 2 commit(s), 7 file(s) changed. Surface: changes, installer. Reaches an existing org: no.*

---

107 prove cases that could never fire, and nothing was watching

`bin/prove` finished for the first time in this session and reported one
SETUP-FAIL. Asking what else was in that class found 23 rows naming a literal
the strip deleted, on top of the 84 naming a deleted file removed earlier — 107
dead cases in a suite of 478. Every one names an assertion that no longer exists
in bin/check, so all of it is residue, not a lost rule.

THE REAL DEFECT IS THAT NOTHING CHEAP LOOKED. prove reports both classes, but it
takes ~40 minutes and refuses to start while check is red, so the report reached
nobody — and the first dead case raised FileNotFoundError out of a worker and
took the run down before it could say so. A detector that only runs in the suite
it disables is not a detector.

bin/check now parses bin/prove's case tuples statically and fails on any row
naming a file that does not ship or a literal absent from it. Seconds, every
commit. Proven by planting a dead case: it fires, names the line and the path.

check 1123 passed · 0 failed · 4 skipped.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---

Regenerate the change record for 1.32.1

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---
