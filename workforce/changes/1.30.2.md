# 1.30.2

<!-- Enforcement (maintainer-facing; bin/ does not ship — on a host this is `/workforce verify`): 0 assertion(s) in bin/check name this file; 6 normative claims total. 8 generic assertions guard it too. Coverage is a floor, not a certificate. -->
*Released 2026-09-09. 2 commit(s), 261 file(s) changed. Surface: handbook, chart, principles, audit, verify, hooks, scripts, installer, platform. Reaches an existing org: YES.*

**An org built before this release carries a stale copy of something this release changed.** Updating the skill is not enough — run `/workforce audit` so the heal passes rewrite it.

---

Bump to 1.30.2 — the strip shipped on an unchanged version

Commit 90fb341 removed 60 shipped files and modified 23 (134 -> 101 files,
32 -> 19 scripts, 9 -> 1 hook) and its subject line says v1.31.0, but
`workforce/references/version.md` and the `SKILL.md` frontmatter mirror both
still read 1.30.1. Nothing on a host could tell the two trees apart:
`/workforce version --check` compares the number, the installer's downgrade
guard compares the number, and `update` reports before-and-after from the
number. A user on the pre-strip 1.30.1 would have been told they were current
while running 60 files that no longer exist upstream.

1.30.2, not 1.31.0, on the user's call.

Both anchors move together, which is what `bin/check`'s equality assertion and
`bin/pre-commit-version` require.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_014wztAHRA71wkfjGF53BkUS

---

A rule written sixteen times is a rule that is not working (v1.31.0)

The project had grown to 2.6 MB across 134 shipped files, and the user reported the
model "talking confused". The first diagnosis blamed a wording clause in
`wf-speak-guard`. That was too narrow: a different clause of the same guard had been
removed the night before, measured against 2,149 real replies, and the complaint
arrived anyway.

MEASURED, and this is what the strip is actually for. `SKILL.md` is read at the head
of every turn and is 78,135 B: 7,012 B of the user's own quoted words, 32,532 B of
commentary explaining them back, 24,367 B of enforcement gates. Nine percent of the
always-on text is the user. `wf-context` on this repo's own employees: doctrine-author
receives 98,203 B before its task, 83,820 B of it preloaded material it never asked
for. And the plain-output rule alone was carried by sixteen shipped files. A rule
stated sixteen times and still not landing is not under-enforced.

REMOVED, on the user's explicit marks: the speech guards and their reference, four
always-on hooks, the git pin guard, handoff, the two budget producers, eight
rarely-run commands, the two personnel hooks, and the five canary fixtures that were
registered as agents and therefore appeared in every session's agent menu.

134 -> 101 shipped files. 32 -> 19 scripts. 9 -> 1 hook. 34 -> 26 commands.
`assets/` untouched, as instructed.

BEYOND THE MARKS, three calls, each reported rather than left to be found:

`wf-budget-guard` also goes. It was marked Keep, but it reads what `wf-model-budget`
and `wf-effort-budget` emit and both were on the removal list. Tested after the fact:
it fires on every AskUserQuestion and returns "passing through" every time — the
dormant shape `enforcement.md` refuses.

The handbook length ceiling is RESCUED, not deleted. `delegation-budget.md` held a
MEASURED 172-line constant that ten files check against; deleting the file would have
left all ten checking nothing while still reporting a pass. Relocated verbatim into
`procedure-for-procedures.md`, where a handbook is authored.

The user's directive block is untouched. Five citations inside it now point at deleted
files. Directive one is that the user's verbiage is retained; a dated attribution is
not rewritten to tidy a link.

TWO NEW MECHANISMS, because a removal that reaches only this repo reaches nothing:

`install` now PRUNES. It had no removal operation of any kind and never compared
against the tree already on disk, while `update.md` line 57 promised "a clean full
replacement, not a merge". That promise was false and read clean only because every
release from 1.8.0 to 1.30.1 mostly ADDED files. This is the first that removes many.
Scope is the safety argument: inside the skill directory the manifest is authoritative;
outside it only files a previous run recorded writing are touched; `.claude/workforce/`
project state is never walked. Proven in a fixture — three orphans removed, two recorded
external files removed, a user's own agent in the same directory SURVIVED, and all three
refusal guards destroy nothing.

`audit` gains Step 5h, which heals an org built under the old shape: unwires
registrations whose script is gone, collapses registrations differing only by
config-directory spelling, clears an output style whose file is absent, unregisters
fixtures that no longer ship, repoints handbooks citing deleted references. It
identifies a dead hook by its target being absent, never by a name list this file would
have to keep current.

HEALED: 171 dangling references across 31 files. `handbook-templates.md` was
instructing employees to run a script that no longer exists.

FIXED IN THE SUITE, both pre-existing: `bin/check` crashed outright on the legal
`frozenset()` empty form, which would have silenced every assertion after it; and
`census-deadhook` read the developer's personal config directory, so it reported a
different number for every person who ran it.

bin/conformance 85 assertions 0 failed. bin/script-conformance 176 passed 0 failed.
bin/check 1078 passed, 1 failed — `~/.claude` is 23 files behind and `bin/sync
--personal` only reaches the config directory a session runs under. Remedy:
`WORKFORCE_REPO_URL="file://$PWD" ./install --user`, which also exercises the new
prune against a real tree.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01J2VxPo1pdbWvfPVPSJKi9u

---
