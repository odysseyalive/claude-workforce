# 1.66.0

<!-- Enforcement (maintainer-facing; bin/ does not ship — on a host this is `/workforce verify`): 0 assertion(s) in bin/check name this file; 0 normative claims total. 8 generic assertions guard it too. Coverage is a floor, not a certificate. -->
*Released 2026-09-22. 3 commit(s), 7 file(s) changed. Surface: changes, installer, platform. Reaches an existing org: YES.*

**An org built before this release carries a stale copy of something this release changed.** Updating the skill is not enough — run `/workforce audit` so the heal passes rewrite it.

---

Release 1.66.0: the code lane and Opus pool row run claude-opus-5-5

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>

---

The code lane and the Opus pool row move to claude-opus-5-5, and the pool reorders

By user directive, 2026-09-22. `claude-opus-5-5` replaces `claude-opus-5` as the
Opus row of § Model statics and as the `code` lane's pick; `claude-opus-5` leaves
the pool and stays reachable by hand through the budget's "Other" field. The
"Recommended for code" mark moves with the model.

The pool reordered, which the user's ask did not anticipate and the model
reference decided. Verified against the `claude-api` skill's model table:
`claude-opus-5-5` is $4/$20 per MTok, 1M context, 128K max output, all five
effort rungs `low`-`max`. At $4/$20 it is cheaper than the three $5/$25 Opus
rows, so § Model statics' own rule — ordered by cost, most expensive first —
puts it last. The Opus row falls from 2 to 4, `claude-opus-4-8` and
`claude-opus-4-6` move up, and the code lane's pick is now the cheapest row in
the pool rather than the second-dearest. Two prose statements that read row
numbers are re-pointed with it (the "Row 2 carries two annotations" sentence,
and platform.md fact 12d's § Model statics row citation).

One behavior does not carry over, and it is the reason this is more than a
string swap: `claude-opus-5-5` defaults to effort `medium`, where
`claude-opus-5` defaulted to `high`. § Effort statics had called the code lane's
recommended `high` "the API default"; that sentence was true of the old pick and
false of the new one, so `high` is now stated as the deliberate raise it has
become. platform.md fact 12c carries the rung set and the default, DOCUMENTED
and dated 2026-09-22, with no behavioral canary — unchanged in status.

Sentences that record history are left standing as history: the 2026-09-05
directive's arithmetic, the Lead row's 2026-09-05/09-09 account, and fact 12c's
earlier re-reads. The 2026-09-22 revision is recorded beside them in the same
form.

Fixed in the same pass: § Model statics offered `claude-opus-4-8` as a worked
example of a model reachable only through "Other" while that model was row 3 of
the pool — stale since the 2026-09-09 directive returned it. `claude-opus-5` now
fills that slot, which it genuinely occupies.

bin/check: 1402 passed, 1 failed, 6 skipped (the failure is the pre-existing
personal-install drift, which only `bin/sync --personal` clears and which is the
operator's to run). Under an isolated HOME: 1399 passed, 0 failed.
bin/prove --since HEAD: 26 of 26 PROVEN, 0 VACUOUS.
conformance 85/85, script-conformance 310/310, idempotence 8/1 (the one
non-idempotent writer, `coverage`, fails identically at HEAD).

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>

---

Regenerate the change record for 1.65.1

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TNjva8cmTu8egJrvepMR8W

---
