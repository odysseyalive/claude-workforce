# 1.32.7

<!-- Enforcement (maintainer-facing; bin/ does not ship — on a host this is `/workforce verify`): 0 assertion(s) in bin/check name this file; 0 normative claims total. 8 generic assertions guard it too. Coverage is a floor, not a certificate. -->
*Released 2026-09-09. 2 commit(s), 7 file(s) changed. Surface: changes, hooks, installer. Reaches an existing org: no.*

---

The hook table described three hooks on a one-hook install

Panel cleanup. Two findings survived checking; one did not, and that is recorded
rather than quietly dropped.

DEAD BINDINGS. bin/check carried 25 `read()` calls against files that no longer
ship — wf-speak-guard, wf-standing-request, wf-plain-guard, wf-handoff,
plain-output.md and fifteen more — plus seven variables derived from them. All
32 removed, with the twelve dependent statements that referenced them.

One assertion went with them: `budget wording: neither emitter carries its own
copy of the question copy`. It read wf-model-budget and wf-effort-budget, both
deleted in the simplification release, so it had been comparing two empty
strings. It is named here because a silently vanishing assertion is the same
defect as a silently vacuous one.

WHAT DID NOT SURVIVE CHECKING. Two panel members reported that "every assertion
reading them tests the empty string." Parsed: ZERO assertions read those
variables — the assertions had been deleted with the scripts, and what remained
were orphan bindings. Real residue, but not the vacuous-green defect described.
The panel's finding is corrected here rather than relayed, because relaying an
unverified claim is the failure this whole review is about.

THE HOOK TABLE. `enforcement.md` says "This distribution ships exactly one hook."
`hooks.md` tabled three: wf-protect-directives, wf-budget-guard, and prose
calling wf-standing-request "the one hook that adds context". The last two ship
in neither `workforce/bin/` nor `manifest.txt`. A reader following that page
would have wired a command that does not exist — the dead registration this
release's `prune_dead_hooks` was built to clear, authored by the same document
that describes the clearing.

`## The budget guard` now says REMOVED and records what was lost with it: the
render rule it enforced still stands and is carried by prose alone. That is a
real reduction in enforcement, named rather than left for a reader to infer from
an empty table. The "two edit hooks" paragraph described a four-hook set and is
corrected to one.

check 1128 passed · 0 failed · 4 skipped.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---

Regenerate the change record for 1.32.6

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---
