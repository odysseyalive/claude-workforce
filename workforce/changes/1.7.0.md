# 1.7.0

<!-- Enforcement (maintainer-facing; bin/ does not ship — on a host this is `/workforce verify`): 0 assertion(s) in bin/check name this file; 3 normative claims total. 8 generic assertions guard it too. Coverage is a floor, not a certificate. -->
*Released 2026-09-02. 1 commit(s), 24 file(s) changed. Surface: handbook, principles, audit, verify, scripts. Reaches an existing org: YES.*

**An org built before this release carries a stale copy of something this release changed.** Updating the skill is not enough — run `/workforce audit` so the heal passes rewrite it.

---

context-wiring: the reference material the ask draws on (v1.7.0)

Sophisticated orgs are gaslit by their own repo: agents and the CEO don't
know the reference material an issue needs is present, author from the thin
surface in front of them, then report content as "structurally deleted"
when it was recoverable in seconds. v1.6.0 carried the ASK down the chain;
this wires each craft to the reference material the ask draws on.

- `## Sources` handbook section (IC/CEO/Lead) + constitution item 10: the
  coupled anti-gaslighting/ask rule — search Sources before reporting
  anything missing; if unresolved, QUESTION up the chain, never fabricate
  or guess. Receiving side: escalate, never absorb-and-guess.
- org-design.md Sources criterion: evidence-cited, root/glob-level,
  never-invent; two source types (read-only paths; read-write stores via
  Skill(<gateway>)); channel integrated per the two-path design.
- Seam-completeness: a subordinated concern heals by amendment, not a hire.
- audit.md Step 5e heals existing orgs in-run (reverse Sources census as
  detector, load-bearing rank), riding v1.6.0's Step 5d + version stamp;
  verify coverage-set, handbook.md/hire.md wiring, anti-drift (Sources is
  the one authored record, everything else derives it). INV-SOURCES/SEAM.
- Enforcement: 19 paired bin/check + bin/prove cases, all proven to
  discriminate. Proving caught a misrouted del-case (discovery ->
  discharge.md, would read VACUOUS) — fixed with a DISC constant.
- wf-conform Sources advisory + 4 fixtures; swept 4 stale count-pinned
  conformance fixtures (added a `1 governed` floor). Directive captured
  verbatim in SKILL.md. 1.6.0 -> 1.7.0.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_012UyA4C7gnCy2DegZejUyT7

---
