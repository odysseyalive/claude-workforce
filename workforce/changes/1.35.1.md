# 1.35.1

<!-- Enforcement (maintainer-facing; bin/ does not ship — on a host this is `/workforce verify`): 0 assertion(s) in bin/check name this file; 0 normative claims total. 8 generic assertions guard it too. Coverage is a floor, not a certificate. -->
*Released 2026-09-09. 2 commit(s), 10 file(s) changed. Surface: audit, changes, hooks, installer. Reaches an existing org: no.*

---

A rule whose spec, producer and reversal were all deleted, still printed every run

T-SWEEP, the collision sweep, run against the finished build. One real finding,
and the cleanup cascaded through four checks that were holding it in place.

INV-HANDOFF was a live invariant with nothing behind it. `audit` Step 5g
instructed every run to execute `wf-handoff --wire --execute` — a script the
simplification release deleted — to record state in a `.handoff-wired.json`
nothing writes, and to name a reversal nothing can perform. Its spec file,
`references/handoff.md`, went with the script. The step then printed
`INV-HANDOFF leads 3 · clause present 3` as though a measurement had been taken.

A rule whose spec, producer AND reversal are all gone is not a weakened rule; it
is a step that fails on every run. Removed: the step, the invariant row, the
counted line, and the declared total (Thirty-eight → Thirty-seven).

AND A bin/check ASSERTION WAS HOLDING IT THERE. "edges: audit Step 5g heals
existing orgs, and prints INV-EDGES + INV-HANDOFF" REQUIRED the dead pairing to be
present — a guard outliving its subject and keeping it alive. It now asserts the
live edge heal and that INV-HANDOFF must NOT return.

hooks.md also stated a hook count in prose beside the table that IS the count. The
table is the count now; bin/check derives the set from SHIPPED_HOOKS and requires
it to match the table and the manifest. That was a fourth place for one fact to
drift, three days after three of them disagreed.

ON THE SWEEP ITSELF, because the method matters more than the finding: the first
numeric detector reported 24 collisions and nearly all were false — "one agent
hop", "the one procedure that owns". A tight inventory predicate reported ONE. The
first pass was not run past the user as findings; an over-broad detector that
reports noise is the 7% failure in a different costume, and the honest output of a
sweep with bad precision is a better sweep, not a longer list.

Also measured and NOT acted on, deliberately: five `references/…md` citations that
do not resolve. Two are project-internal paths the regex mis-parsed. Two are inside
the immutable directive block, where 1.31.0 already ruled that "a dated attribution
is not rewritten to tidy a link" — directive one is retention, and those citations
are the user's own words pointing at files this project chose to delete.

check 1135 passed · 0 failed · 4 skipped. script-conformance 189/189.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---

Regenerate the change record for 1.35.0

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---
