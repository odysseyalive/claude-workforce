# 1.32.1

<!-- Enforcement (maintainer-facing; bin/ does not ship — on a host this is `/workforce verify`): 0 assertion(s) in bin/check name this file; 1 normative claims total. 8 generic assertions guard it too. Coverage is a floor, not a certificate. -->
*Released 2026-09-09. 2 commit(s), 7 file(s) changed. Surface: changes, installer. Reaches an existing org: no.*

---

No .claude/ here is the settled state, not a step somebody skipped

The skip messages added an hour ago closed with "Precondition: `/workforce dev
audit` in this repo" — which reads as a nudge to create the very thing the user
deleted on purpose. That is the same defect this release fixed in update.md step
7, one layer down: naming `audit` where nothing needs auditing.

The user's own words: "I deleted the local .claude directory here, because it
shouldn't be needed at all." So absence is a decision, and every skip now says
so and names where the rule IS measured instead — in a project that carries an
org. The doctrine-author skips also state that the user's Non-negotiables are
not at risk, because a reader seeing "not evaluated" beside "the user's own
words" should not have to wonder.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---

Regenerate the change record for 1.32.0

`bin/gen-changes` projects the index and the per-release bodies from git
history, so it runs after the commit it describes, never before.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---
