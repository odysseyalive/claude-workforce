# 1.9.0

*Released 2026-09-02. 2 commit(s), 26 file(s) changed. Surface: audit, hooks, scripts, installer. Reaches an existing org: YES.*

**An org built before this release carries a stale copy of something this release changed.** Updating the skill is not enough — run `/workforce audit` so the heal passes rewrite it.

---

wf-budget-guard: a hand-built budget picker is blocked, not restated (v1.9.0)

Measured 2026-09-02 on a v1.8.0 install: a re-audit of apps-odyssey-alive
read the project's stale `## Model statics`, offered the retired frontier
pick as a static, marked the current pin (Recommended), and never ran
wf-model-budget — the third time the same drift shipped past the same prose.

wf-budget-guard is a PreToolUse hook on AskUserQuestion, in the default set.
A model picker (two or more model IDs among the options) or an effort picker
(two or more rungs) must equal one of the emitter's LANE blocks for the
project — same IDs or rungs, same order, the one (recommended) mark on the
same option — or the call is blocked with the offered set, the nearest
emitted lane, any STALE-ARTIFACT line, and the command to run. No model ID
or rung lives in the hook; it reads the emitters. Fails open on its own
faults only; an emitter refusal blocks.

Wired through manifest, wf-settings-apply's registry, hooks.md (row plus
§ The budget guard), an enforcement.md row at its true strength ("PREVENTS
once wired"), and audit-setup.md § Step 0.4a beside the rule it enforces.
Seven fixtures; three checks; six prove cases. 376 of 376 proven.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Vv86bzSmHP9dJvpdEBZ8iT

---

prove: follow the two renamed pool checks, and break the new advisor coupling

The 2026-09-02 pool refresh renamed "each lane recommendation is in-pool,
except the sanctioned visual out" (the out is now permitted, not required)
and generalised the audit-setup first-class-lead check; the prove cases
still carried the old names and reported VACUOUS. Renamed both, and added a
case that strips the session-advisor naming from the pool row so the new
advisor-coupling check is proven by breaking. 370 of 370 proven.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Vv86bzSmHP9dJvpdEBZ8iT

---
