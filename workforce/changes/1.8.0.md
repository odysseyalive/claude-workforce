# 1.8.0

*Released 2026-09-02. 1 commit(s), 15 file(s) changed. Surface: audit, scripts, platform. Reaches an existing org: YES.*

**An org built before this release carries a stale copy of something this release changed.** Updating the skill is not enough — run `/workforce audit` so the heal passes rewrite it.

---

model defaults: code, creative-visual, and the session advisor on claude-fable-5-1 (v1.8.0)

claude-fable-5-1 enters the statics pool as row 1, recommended for code AND
creative-visual and named as the session advisor's pick; claude-opus-5 leaves
the pool (recommended for nothing, reachable via Other). The out-of-pool path
is no longer exercised by the shipped template; the mechanism, its fixtures,
and creative-visual's standing as the one sanctioned lane all stay, and
bin/check now permits an out on that lane instead of requiring one.

Both budget emitters read every `Recommended for` annotation a row carries
(_rec_lanes), and bin/check's lane parser reads per row; fixtures
modelbudget-multilane-row and effortbudget-multilane-row prove it. A new
check couples § Session advisor's default cell to the pool row that names it.

Effort values unchanged, receipt added to § Effort statics from the model
reference's measured curves: code high (steep coding curve), creative-visual
medium (flat generative curve), advisor none. platform.md fact 12c re-pointed
to claude-fable-5-1.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Vv86bzSmHP9dJvpdEBZ8iT

---
