# 1.39.1

<!-- Enforcement (maintainer-facing; bin/ does not ship — on a host this is `/workforce verify`): 0 assertion(s) in bin/check name this file; 1 normative claims total. 8 generic assertions guard it too. Coverage is a floor, not a certificate. -->
*Released 2026-09-09. 2 commit(s), 14 file(s) changed. Surface: audit, verify, hooks. Reaches an existing org: YES.*

**An org built before this release carries a stale copy of something this release changed.** Updating the skill is not enough — run `/workforce audit` so the heal passes rewrite it.

---

Bump to 1.39.1 — the healing pass shipped on an unchanged version

The commit titled v1.39.1 landed with version.md and SKILL.md still reading 1.39.0,
so a host updating would have fetched the healed references under a version number
that says nothing changed. Caught by reading the file rather than the commit title.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---

The heal reported 171 and missed four (v1.39.1)

FOUND BY `wf-live`, the registry shipped hours earlier in 1.39.0, on its first run
against this tree. The simplification release (90fb341) removed nine mechanisms on
the user's explicit marks and reported "HEALED: 171 dangling references across 31
files." It missed four, and each one was a live procedure instructing a host to run
a command that does not exist:

  model-map.md          Run `wf-model-budget --root <project> --config …`
  model-map.md          Run `wf-effort-budget --root <project> …`
  verify.md, staging.md Spawn `wf-model-canary` by its registered subagent_type
  audit.md Step 6-G     wf-pin-check --install-hook --root <repo> --execute

MY FIRST READ OF THIS WAS WRONG AND IS CORRECTED HERE. Seeing audit-setup.md still
list the budget pickers as questions 3, 5 and 6 of 6, and invariants.md still carry
INV-PINS as row 21, I concluded the mechanisms had to come back. That was sight
without hearing: the requirements were read and the record that deleted them was
not. `git log -1 90fb341` says the removals were "on the user's explicit marks."
The scripts stay dead; the documents were wrong.

THE ASSERTIONS WERE ENFORCING THE DEFECT. Six of `bin/check`'s 1,145 green
assertions REQUIRED the stale text to be present — "audit-setup: the model budget
option set is produced by wf-model-budget", "pin guard: audit runs wf-pin-check
--install-hook". The suite was holding every host to being told to run deleted
scripts, and it was green the whole time. Each is rewritten to assert the rule that
outlived its emitter: the option set is derived from ONE source, the shipped
template, and no document names a removed script as live.

WORSE, IN THE ONE TABLE THAT FORBIDS IT. `enforcement.md` carried two rows claiming
`wf-plain-guard` and `wf-budget-guard` **PREVENT** things. Neither ships. The table
whose own gate reads "anything in a cannot row must never be described as enforced,
guaranteed, prevented, or sandboxed" was describing two removed hooks as preventing
calls — and a check asserted it kept doing so. Both are CANNOT rows now.

A DIRECTIVE WAS BLOCKED AND NOBODY KNEW. The CLAUDE.md evacuation gates
PASS-CLAUDE-MD-EVACUATED on the cold-reader request having a carrier that is not
CLAUDE.md itself, and named `wf-standing-request` as that carrier, with a literal
command to wire it: `! wf-settings-apply --wire-hook wf-standing-request`. The hook
went in the simplification release. The precondition is not gated, it is
UNSATISFIABLE — no settings write helps, because there is nothing to wire. Recorded
in claude-md.md and passes.md rather than left for the run that hits it, with the
constraint a replacement must meet: `SubagentStart` fires exactly where the request
must be present, which is what the removed `UserPromptSubmit` hook could only
approximate.

RETIRED RATHER THAN DELETED, where a number is load-bearing. audit.md Step 6-G is a
RETIRED placeholder so Step 6-H and Step 6a keep the positions other files cite;
invariants.md row 21 stays parseable and marked RETIRED because five other files
cite invariant rows by number. staging.md § Phase D keeps its whole procedure as
the specification a replacement must satisfy, and reports UNAVAILABLE — which was
already one of its four outcomes. The constraint on any replacement is recorded:
the fixture must be spawnable by type WITHOUT being menu-visible, since appearing
in every session's agent menu in every project is why the five canaries were cut.

LEFT ALONE DELIBERATELY: five citations inside SKILL.md's immutable directive block
still name removed hooks, including `wf-plain-guard` "which BLOCKS". The
simplification release made that call explicitly — "the user's directive block is
untouched … a dated attribution is not rewritten to tidy a link" — and overturning
a recorded decision unilaterally is not a healing pass's business. Reported to the
user instead. `wf-live` lists them in its `stale` tier every run.

BROKEN 4 -> 0. check 1144 passed · 0 failed · 4 skipped. script-conformance 199/199.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TotmxgUcZPnUvbvceFgUje

---
