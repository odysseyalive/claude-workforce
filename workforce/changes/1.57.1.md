# 1.57.1

<!-- Enforcement (maintainer-facing; bin/ does not ship — on a host this is `/workforce verify`): 0 assertion(s) in bin/check name this file; 0 normative claims total. 8 generic assertions guard it too. Coverage is a floor, not a certificate. -->
*Released 2026-09-13. 3 commit(s), 12 file(s) changed. Surface: changes, scripts, installer. Reaches an existing org: no.*

---

Bump to 1.57.1 for the restored standing cold-reader request

- 6647799  the generated CLAUDE.md region carries the standing cold-reader
  request again; it had been empty since 1.31.0 because wf-claude-md loaded it
  from a removed hook. The text now lives in wf-claude-md, and the false "check
  the wf-standing-request hook" message is corrected.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01ByGYhCt5spAiQW7cc7WYJr

---

The generated region carries the standing cold-reader request again

wf-claude-md loaded STANDING_REQUEST from wf-standing-request, a hook removed
in 1.31.0 (90fb341). The loader swallowed the missing file and returned "", so
every generated CLAUDE.md region since then was written without the request,
silently, while claude-md.md, audit-setup.md and the release gate's clause 3b
all said the region carries it: the remedy when a host suppresses subagent
spawning "unless the user asks". The absent-CLAUDE.md path also told users to
check that the removed hook was wired.

The text is now a constant in wf-claude-md, recovered from
`git show 90fb341^:workforce/bin/wf-standing-request` and byte-identical (269
B); the loader is gone. The false message now says the region is the only
carrier and there is no hook. claude-md.md:108 and :193 no longer describe the
hook as live. bin/components' only KNOWN_DANGLING entry is cleared (0 dangling,
0 on record), and bin/prove's two unused constants pointing at the removed hook
are deleted.

A bin/check assertion runs `wf-claude-md --ensure-region --execute` in a
scratch tree and requires the request in the region; its bin/prove case
empties the constant and is PROVEN. Found by the component catalog added in
ef16aca. Built by engineering-coder, work order
org-standing-request-20260913T181247Z. Replayed by the operator: the region on
a scratch project carries all three lines. bin/check 1257 passed, 0 failed
after sync.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01ByGYhCt5spAiQW7cc7WYJr

---

Regenerate the change record for 1.57.0

The record is written from commit messages between bumps, so the bump commit
has to exist before its own body can be generated. Surface: principles,
changes, hooks, scripts, installer. Reaches an existing org: YES, because the
operating-principles default body (templates.md item 14) changed its checklist
sentence again.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01ByGYhCt5spAiQW7cc7WYJr

---
