# 1.59.1

<!-- Enforcement (maintainer-facing; bin/ does not ship — on a host this is `/workforce verify`): 0 assertion(s) in bin/check name this file; 1 normative claims total. 8 generic assertions guard it too. Coverage is a floor, not a certificate. -->
*Released 2026-09-14. 8 commit(s), 25 file(s) changed. Surface: chart, principles, audit, verify, changes, hooks, scripts, installer, platform. Reaches an existing org: no.*

---

Bump to 1.59.1 for the post-release reference and checker cleanup

Closes the range for 6c9c68d, 1abb09f, bbb7acb, 1fce4f2 and f65de8e: the loop guard's
last live references, the restored anchor and path checks, disband step 6b, the
sidecar example, and the VENDORED correction, so gen-changes does not attribute
shipped changes to a 1.59.0 label that predates them.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpTNVEejrHDeJzSvJKwUdd

---

prove: drop nine constants that point at deleted files or duplicate a definition

Each removed name had no use beyond its own assignment; TMPL and DBUD were assigned
twice with identical values. bin/check 1292 passed, 0 failed; bin/prove compiles.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpTNVEejrHDeJzSvJKwUdd

---

Correct the sidecar example, the VENDORED set, and what they were hiding

enforcement.md's .settings-owned.json example used keys no writer produces; it now
shows the four the scripts write, and bin/check asserts that against the write sites.
VENDORED wrongly exempted image-eval and ui-design; removing them surfaced three
unclosed origin blocks and a drifted duplicate in image-validator, all fixed. Dead
prove constants removed. bin/check 1292 passed, 0 failed; prove --since 60/60.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpTNVEejrHDeJzSvJKwUdd

---

Restore the references/ path check and correct disband step 6b

The path-part link check lost its check() call in 90fb341. It is back, resolving a
skill's paths against its own root, skipping the change record and immutable spans,
and sharing one immutable-span regex with the anchor check. Five real broken links
fixed. disband 6b described a creation record nothing writes; it now reports created
data skills and companions and points at restore. bin/check 1273 passed, 0 failed;
prove --since 182/182.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpTNVEejrHDeJzSvJKwUdd

---

Remove the loop guard's last live references and restore the § anchor check

conversion-taxonomy.md told readers to wire a loop guard removed in 90fb341, and
wf-settings-apply kept the stamp machinery that existed only for it. Both are gone.
The § anchor check that would have caught this lost its check() call in the same
commit; it is restored, skips the change record and immutable blocks, and fixed the
nine broken citations it found. bin/check 1272 passed, 0 failed; prove 172/172.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpTNVEejrHDeJzSvJKwUdd

---

Wire the stale-PRODUCERS detector into a check and drop three dead rows

_stale was built and never read, so a PRODUCERS row whose producer no longer names its
artifact passed silently. It now fails by name, matches on the path under
.claude/workforce/ rather than the basename, and covers unread artifacts too. Removed
.spawn-count, loop-guard.json and .handoff-wired.json: nothing outside the change
record reads or writes them. bin/check 1271 passed, 0 failed; prove --since 9/9.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpTNVEejrHDeJzSvJKwUdd

---

The artifact-producer scan skips the change record

bin/check at a957bec failed "artifacts: every artifact a shipped file reads has a
named producer" on .claude/workforce/commitments.json. The only file naming it was
workforce/changes/1.59.0.md, which quotes 3592d04's commit message. A quoted commit
message names a path; it does not read one.

Class fix: the scan now skips ARCHIVE, the existing set that already keeps the change
record out of the authored-prose checks. No PRODUCERS row was added. With the archive
excluded, no shipped reader of commitments.json exists, and the map is meant to ask
"what writes it?" only when a reader appears. The record itself is unchanged.

Two bin/prove cases pin the fix. Appending an unmapped artifact to org.md still fails
the check, and deleting the ARCHIVE exclusion turns it red again on 1.59.0.md.

- bin/check (throwaway HOME): before 1269 passed, 1 failed; after 1270 passed, 0 failed.
- bin/prove --since HEAD: 8 of 8 PROVEN. bin/prove "artifacts: every artifact": 2 of 2.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpTNVEejrHDeJzSvJKwUdd

---

Regenerate the change record for 1.59.0

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WpTNVEejrHDeJzSvJKwUdd

---
