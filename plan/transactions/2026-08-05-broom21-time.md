# Log 70 minutes against a client project — 2026-08-05

**Ask:** `/org please add time for Broom21 on July 20th for site development: 8:30-9:40`
**Project:** `/home/francis/lab/odyssey-alive`
**Outcome:** worked, and cost far more than it should have.
**Cost:** 3 agent runs (3m57s + 9m9s + a nested follow-up), ~14 minutes, 1 user round trip.

The first transaction submitted here, and the origin of the pass catalog. Every finding below was
reproduced by hand before being recorded.

## What happened

1. Main session dispatched to `@agent-business-books` (T3 IC) at rung 3.
2. It wrote the org CLOCK line, then escalated on two blockers: `is_billable` unspecified, and
   *"the /timesheet push path needs id-lookup + task-matcher spawns that business-books can't make"*.
3. The user answered "billable, push it to Zoho".
4. Main session dispatched to `@agent-business-lead` (T2), which dispatched **back down** to
   business-books to execute the POST.

Every `Bash` call in the run also printed a hook error for a script that does not exist.

## Findings — verified

**Neither blocker was real.**

- The org file already carried both IDs the second agent was spawned to resolve.
  `/home/francis/org/broom21.org`, on the exact headline being edited: `:ZOHO_TASK_ID:
  1117916000006981004` and `:ZOHO_PROJECT_ID: 1117916000006979010`, three lines above the LOGBOOK.
  `task-cache.json` held the same task ID as an exact-name hit. `timesheet/SKILL.md:121` names the
  non-spawn path in the same paragraph as the spawn.
- `is_billable` was documented on disk in four files, `entry-patterns.md:8` reading
  `| **is_billable** | true (default) |`. A human was asked a question a file answered.

**The cause is one self-contradicting paragraph.** `business-books.md:49` says *"`/timesheet` is not
yours to invoke"* — unconditional — and then, in the same numbered step, applies the correct
DATA/JUDGMENT test to `/zoho` and `/ynab`. Its bound `operating-principles` skill says of the DATA
branch: *"Do not stop."* Attribution is DOCUMENT.

**A capability was invisible to its dispatcher.** `/timesheet` was omitted from the org chart's
`## Mechanicals`, so rung 8 forbade naming it. The same chart says 17 lines earlier that
retained-but-unreduced skills *"remain invocable, so they are rows here."*

**An edge went unrecorded.** Three dispatches, two `.spawn` files. The lead→books edge has none, so
`review` cannot see it — and that edge is the org chart's only backstop.

**Three dead hook registrations** pointed into `code-evaluator/hooks/`, a directory that has never
existed. Detected by `wf-census`; the remedy `verify.md:117` names is one `hooks.md:167` cannot
deliver.

## What generalised, and what did not

**Did not generalise — the founding candidate.** "An agent that is really a script" looked like the
headline class: `timesheet/agents/id-lookup` is `allowed-tools: Read, Grep` and reads a row from a
static table. But three `id-lookup` agents on that tree are byte-identical on every mechanically
keyable field and semantically different — `zoho/agents/id-lookup:40` encodes a **user directive**
(*"FORBIDDEN … Per user directive"*). Auto-converting on the obvious proxy destroys it.
`DEF-2026-08-05-mechanism-partition-ic-tier` had already measured the class at **5 true of 22**.

That result set the catalog's default verdict to `REPORT` and produced its governing sentence:
**detection generalises; remediation does not.**

**Did generalise — two passes**, neither of which is what the transaction was about, and both found
by pointing the proposed detectors at this repository:

- `PASS-MARKER-GRAMMAR` — `plan/marker-grammar-2026-08-05.md`
- `PASS-DEAD-SCRIPT` — `plan/dead-script-2026-08-05.md`

## The instance defects, still open

Recorded here so they are not lost behind the passes. None is closed by this change.

1. Narrow `business-books.md:49` to the DATA/JUDGMENT test it already applies to `/zoho` and `/ynab`.
2. Convert `timesheet/agents/id-lookup` to a script; re-open `zoho` and `ynab` for the same shape,
   knowing `zoho`'s carries a directive and is not a mechanical conversion.
3. `/timesheet` as two `## Mechanicals` rows — `status` read-only, the write modes `Destructive: yes`.
4. `verify.md:117` names a remedy `hooks.md:167` cannot deliver: consumer named, producer absent.
5. `INV-HOOKS` is scoped *"after hook relocation"* but gates on all dead wiring, so it blocks the
   sweep for a reason outside its own charter.
6. Count escalations in `review`, classified as tool-grant / spawn-ceiling / scope gap. Only the third
   is a reason to split an employee.
7. `default_billable` per project, proposed by business-books and endorsed by its lead in the run's
   own OUTPUT.md, which went nowhere because nothing routes an improvement observation.
