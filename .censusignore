# Declared test corpus — deliberately malformed trees the census must not survey.
# See wf-census.load_ignore(). Absent is the normal case; this repo is unusual.
fixtures/

# Foreign local imports — gitignored overlays vendored from odyssey-alive, NOT
# claude-workforce content. `voice-text-eval` references
# `steganographer/scripts/scrub.js`, which resolves only in that other repository;
# surveying these here produced a false PASS-DEAD-SCRIPT "regression". They are
# never committed, never converted, never swept — declared out so the survey stops
# treating a foreign overlay as this project's own work.
.claude/skills/voice
.claude/skills/voice-text-eval
