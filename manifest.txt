# Claude Workforce install manifest
#
# The single authoritative list of every file the installers ship.
# Consumed by BOTH installers: `install` (bash, Linux/macOS) and
# `install.ps1` (PowerShell, Windows). Add every new shipped file here;
# there are no per-installer fetch loops to keep in sync.
#
# Format: [flag] <path relative to repo root>
#   (no flag)  fetch unconditionally, overwriting any existing copy
#   keep       fetch only if absent on the host (user-editable file; never clobber)
#   hook       fetch unconditionally; chmod +x. Registered to a harness event by
#              `/workforce hooks --execute`; `verify` reports whether it is wired
#   exec       fetch unconditionally; chmod +x. Invoked by a procedure for its
#              exit code; registered to nothing
#   style      fetch unconditionally into <config-root>/output-styles/ (personal) or
#              .claude/output-styles/ (project) — NOT the skill directory. An output
#              style is only found there. Selected by `outputStyle` in a settings
#              file, which `/workforce hooks --execute` writes
#   canary     fetch unconditionally into <config-root>/agents/ (personal) or
#              .claude/agents/ (project) — NOT the skill directory. A measurement
#              fixture, not an employee: it must be REGISTERED as an agent before
#              `staging.md` Phase C or D can spawn it, and it is shipped so the first
#              audit finds it already there rather than having to write it
#
#   retired    NOT fetched, and not a shipped file. A path this project USED to
#              install OUTSIDE the skill directory and no longer ships. It exists so
#              the prune can reach a host whose `.installed-external` record predates
#              the record itself — see § Retired external paths at the foot of this file
#
# THIS LIST IS THE FLAG SET, and it is complete. Both installers branch on all six
# and every reader that strips a flag must strip all five. `canary` went undocumented
# here from the day it was added until 2026-09-07 while five rows used it, and
# release-verifier's own completeness check — which its handbook orders to read the
# set "off manifest.txt's own header, never from memory" — would have derived four
# flags and mis-stripped every canary row, reporting five present files as ABSENT.
# That is the same false-ABSENT failure `style` caused two days earlier, reproduced
# by the instruction written to prevent it. Found by the cold reader probing
# release-verifier.
#
# NOTE on `hook` vs `exec`. Mechanically identical — both chmod +x — and two words
# because the DISTINCTION IS THE THING BEING TRACKED, not the file mode. A `hook`
# fires unprompted and needs a wiring command and a wired/orphaned report; an
# `exec` is called by name and its absence is loud on the next invocation.
# references/enforcement.md § Nothing ships dormant draws the line.
#
# `keep` has no users: no shipped file is user-editable, because project state
# lives in the project (references/scopes.md). It is retained because the grammar
# is shared with claude-enforcer's installers; not a dead branch to "clean up"
# without checking that shared grammar first.
#
# HISTORY, because this line moved twice on 2026-08-03 and the second move
# corrected the first. The four inherited hooks were deleted for good reasons and
# the deletion was then generalized into "ships no executables" — which forbade
# the scripts too, so `audit` Step 1b hand-derived a census and `verify`
# re-specified in prose what a script asserts, in the project whose second
# directive is to automate everywhere feasible. Narrowing that to "ships no hooks"
# was the same over-reach one level down: the finding underneath was never about
# file type, it was that A MECHANISM SHIPPING UNWIRED ENFORCES NOTHING AND LOOKS
# LIKE IT DOES. So hooks ship, with the wiring command whose absence was the
# actual defect.
#
# Host destination: .claude/skills/workforce/<path minus leading "workforce/">

workforce/SKILL.md
workforce/.immutable.sha

# ── Scripts ───────────────────────────────────────────────────────────────────
# Invoked by a procedure and read for an exit code. Registered to nothing. Each
# was released by being made to fail — negative tests recorded in
# plan/ship-the-scripts-2026-08-03.md, per data-skills.md § Maintainers.
exec workforce/bin/wf-census
exec workforce/bin/wf-checkrun
exec workforce/bin/wf-companion
exec workforce/bin/wf-conform
exec workforce/bin/wf-context
exec workforce/bin/wf-runlength
exec workforce/bin/wf-permissions
exec workforce/bin/wf-remainder
exec workforce/bin/wf-seed
exec workforce/bin/wf-ratchet
exec workforce/bin/wf-apply
exec workforce/bin/wf-preflight
exec workforce/bin/wf-settings-apply
exec workforce/bin/wf-skill-match
exec workforce/bin/wf-stamp
exec workforce/bin/wf-ledger

# ── Hooks ─────────────────────────────────────────────────────────────────────
# Registered to a harness event by `/workforce hooks --execute`, which is the
# mechanism whose ABSENCE killed the four inherited hooks: they shipped dormant
# because the command documented to wire them did not exist. `verify` reports
# wired / orphaned / dead-wiring, so dormancy is now visible rather than assumed.
#
# One Python file, not a bash/PowerShell pair. claude-enforcer ships both variants
# and its own CLAUDE.md records that the .ps1 ports "have NOT yet been executed on
# a real Windows host" — two implementations, one of them unmeasured. Python is
# already a shipped dependency (the two scripts above), so one file is one
# behavior on both platforms.
hook workforce/bin/wf-protect-directives

# ── Output styles ─────────────────────────────────────────────────────────────
#
# The plain-speak rule, carried where it cannot decay. Output styles modify the
# system prompt, so the rule is present at full strength on turn 200 exactly as
# on turn 2 — unlike CLAUDE.md, a constitution item, or a dispatch rung, each of
# which is read once at the head of a context and then competes with everything
# newer. references/plain-output.md § Where the rule is carried.

# ── References ────────────────────────────────────────────────────────────────
workforce/references/principles.md
workforce/references/prior-art.md
workforce/references/platform.md
workforce/references/scopes.md
workforce/references/audit-setup.md
workforce/references/org-design.md
workforce/references/org-doctrine.md
workforce/references/procedure-for-procedures.md
workforce/references/handbook-templates.md
workforce/references/org-chart-format.md
workforce/references/conversion-taxonomy.md
workforce/references/conversion-department.md
workforce/references/legacy-markers.md
workforce/references/discovery.md
workforce/references/invariants.md
workforce/references/passes.md
workforce/references/records-ownership.md
workforce/references/data-skills.md
workforce/references/personnel-templates.md
workforce/references/verification.md
workforce/references/evaluators.md
workforce/references/image-eval-seed.md
workforce/references/recruiter.md
workforce/references/recruit-seed.md
workforce/references/ui-design-seed.md
workforce/references/staging.md
workforce/references/deferred.md
workforce/references/enforcement.md
workforce/references/personas.md
workforce/references/templates.md
workforce/references/session-artifact.md
workforce/references/version.md

# org-config.template.md is the SHIPPED TEMPLATE for the per-project config
# (tier -> model/effort, department roster, per-project markers).
#
# It is fetched UNCONDITIONALLY and is never edited by the user. `/workforce audit`
# instantiates it once into ${CLAUDE_PROJECT_DIR}/.claude/workforce/org-config.md
# and never clobbers that copy afterwards.
#
# Why a template rather than claude-enforcer's `keep`-flagged in-skill file: the
# skill may be installed at USER scope (~/.claude/skills/workforce/), where a single
# in-skill config would be shared by every project on the machine — department
# rosters and tier->model tables silently colliding across unrelated orgs. `keep`
# protects a file from re-download; it cannot protect it from being in the wrong
# scope. Project state lives in the project. See references/scopes.md.
workforce/references/org-config.template.md

# ── Vendored evaluator catalogs ───────────────────────────────────────────────
# The portable corpora, copied VERBATIM from claude-enforcer's shipped set. This
# project supersedes it, so the catalogs come with it — until 2026-08-03 workforce
# described a propagation mechanism and shipped nothing to propagate, which made a
# fresh install on any machine without the predecessor strictly thinner than the
# system it replaces. That is the one outcome the standing directive forbids.
#
# VENDORED, NOT AUTHORED. Each keeps its own version anchor on line 1
# (`creative-scrub-ref-version`, `code-eval-ref-version`) — that anchor is what
# forcible propagation compares against, and rewriting these files here would fork
# a corpus that has one origin. Update them by re-copying from the source and
# bumping nothing by hand.
#
# Not vendored: `code-evaluator/skill-template.md`. It instantiates a skill for
# skill-builder's own `code-eval create`; workforce authors that wrapper itself
# (`references/evaluators.md` § The structure).
workforce/references/catalogs/text/text-tells.md
workforce/references/catalogs/text/version.md
workforce/references/catalogs/code/version.md
workforce/references/catalogs/code/cross-file-detection.md
workforce/references/catalogs/code/mistake-taxonomy.md
workforce/references/catalogs/code/native-tool-map.md
workforce/references/catalogs/code/guards.md
workforce/references/catalogs/code/gotchas.md

# ── Authored security catalog ─────────────────────────────────────────────────
# AUTHORED-AND-CITED, a third provenance distinct from the two blocks around it.
# Unlike the vendored catalogs above (byte-identical imports from claude-enforcer),
# this catalog is authored by workforce and DISTILLED from three upstream repos —
# `OWASP/CheatSheetSeries`, `swisskyrepo/PayloadsAllTheThings`, `semgrep/semgrep-rules`
# — cited by CWE and source rather than copied verbatim (a 37 MB verbatim copy would
# defeat the selective-loading the catalog exists for). A re-distillation re-reads
# those pins (see `catalogs/security/version.md`) and appends only new flaw classes;
# it is not compared against a claude-enforcer corpus. So it may be freely edited
# here — there is no single-origin corpus to fork — which the VENDORED rule forbids
# and the additions-slot rule below does not fit either.
workforce/references/catalogs/security/version.md
workforce/references/catalogs/security/security-taxonomy.md
workforce/references/catalogs/security/native-tool-map.md
workforce/references/catalogs/security/guards.md
workforce/references/catalogs/security/cross-file-detection.md

# ── Authored evaluator additions ──────────────────────────────────────────────
# AUTHORED HERE, NOT VENDORED — the deliberate counterpart to the block above,
# and the only sanctioned place to GROW a catalog. The vendored corpus may not be
# edited (it must stay byte-identical to its origin so a reconcile can compute a
# difference), and a project's own catalog under `.claude/skills/<evaluator>/`
# does not ship. Before 2026-08-06 those were the only two slots, so an entry
# authored here had nowhere to live that reached an end user, while
# `references/evaluators.md` § Seeding called growing the seed a release task.
#
# These carry their own anchors (`text-additions-version`, `code-additions-version`)
# and are appended AFTER the vendored copy at seeding. The anchor a project writes
# records both provenances separately, so a later reconcile can still tell an
# upstream row from one of ours.
workforce/references/evaluator-additions/text-tells.md
workforce/references/evaluator-additions/code-eval.md

# ── Procedures ────────────────────────────────────────────────────────────────
workforce/references/procedures/audit.md
workforce/references/procedures/org.md
workforce/references/procedures/handbook.md
workforce/references/procedures/principles.md

# HR lifecycle
workforce/references/procedures/hire.md
workforce/references/procedures/retire.md

# The HR loop
workforce/references/procedures/review.md
workforce/references/procedures/amend.md
workforce/references/procedures/defect.md
workforce/references/procedures/ledger.md
workforce/references/procedures/roster.md

# Payroll, budget, measurement
workforce/references/procedures/model-map.md

# Recovery
workforce/references/procedures/backup.md
workforce/references/procedures/restore.md
workforce/references/procedures/rollback.md
workforce/references/procedures/disband.md

# Maintenance
workforce/references/procedures/checksums.md
workforce/references/procedures/verify.md
workforce/references/procedures/hooks.md
workforce/references/procedures/discharge.md
workforce/references/procedures/sweep.md
workforce/references/procedures/intent-router.md
workforce/references/procedures/update.md
workforce/references/procedures/version.md
workforce/references/procedures/preflight.md

# ── Agents ────────────────────────────────────────────────────────────────────
workforce/agents/handbook-cold-reader/AGENT.md
workforce/agents/headcount-skeptic/AGENT.md
workforce/agents/conversion-auditor/AGENT.md
workforce/agents/doctrine-auditor/AGENT.md
workforce/agents/content-classifier/AGENT.md
workforce/agents/provenance-analyst/AGENT.md
workforce/agents/settings-scout/AGENT.md
exec workforce/bin/wf-claude-md
exec workforce/bin/wf-changes
workforce/references/claude-md.md

# ── Retired external paths ───────────────────────────────────────────────────
#
# WHAT THIS IS FOR, and it is one specific hole. `prune_scope` rule 2 refuses to
# delete anything in `agents/` or `output-styles/` unless a previous run of the
# installer recorded writing it, by exact path, in `.installed-external`. That rule
# is right — a basename match would delete a user's own agent that happened to share
# a name — but the record was ADDED with the prune, on 2026-09-09. Every install
# made before that date has no record, so on those hosts rule 2 has nothing to read
# and the prune's first run skips the external files entirely.
#
# MEASURED on this maintainer's own machine, 2026-09-09: `.installed-external` absent
# at `~/.claude/skills/workforce/`, five `wf-canary-*.md` in `~/.claude/agents/` and
# `plain-speak.md` in `~/.claude/output-styles/` — all six shipped by a release that
# no longer ships them, all six invisible to the prune that exists to remove them.
# Five of them appear in the agent menu of every session on the machine.
#
# So the record is SEEDED from this list when it is absent. That keeps rule 2's
# safety property exactly: these are paths this project shipped, named explicitly,
# never inferred from a basename or a directory scan. A user's own agent cannot
# appear here, because the list is authored from this repository's own history.
#
# Destination is derived from the source directory, the same mapping the live flags
# use: `workforce/canary/*` -> the agents directory, `workforce/output-styles/*` ->
# the output-styles directory. A retired path outside those two prefixes is a
# manifest error and `bin/check` fails on it rather than guessing.
#
# A row is added here when a `canary` or `style` row is removed, in the same commit.
# Rows are never deleted: a host can be arbitrarily old, and a list that forgets an
# entry re-opens exactly the hole it was written to close.
retired workforce/canary/wf-canary-a.md
retired workforce/canary/wf-canary-b.md
retired workforce/canary/wf-canary-c.md
retired workforce/canary/wf-ceiling-probe.md
retired workforce/canary/wf-model-canary.md
retired workforce/output-styles/plain-speak.md

# ── Change record (generated by bin/gen-changes — do not hand-edit) ──
workforce/changes/index.md
workforce/changes/1.0.0.md
workforce/changes/1.1.0.md
workforce/changes/1.2.0.md
workforce/changes/1.3.0.md
workforce/changes/1.5.0.md
workforce/changes/1.6.0.md
workforce/changes/1.7.0.md
workforce/changes/1.8.0.md
workforce/changes/1.9.0.md
workforce/changes/1.10.0.md
workforce/changes/1.11.0.md
workforce/changes/1.12.0.md
workforce/changes/1.13.0.md
workforce/changes/1.14.0.md
workforce/changes/1.16.0.md
workforce/changes/1.16.1.md
workforce/changes/1.17.0.md
workforce/changes/1.18.0.md
workforce/changes/1.20.0.md
workforce/changes/1.21.0.md
workforce/changes/1.21.1.md
workforce/changes/1.21.2.md
workforce/changes/1.22.1.md
workforce/changes/1.23.0.md
workforce/changes/1.24.0.md
workforce/changes/1.26.0.md
workforce/changes/1.27.0.md
workforce/changes/1.28.0.md
workforce/changes/1.29.0.md
workforce/changes/1.30.0.md
workforce/changes/1.30.1.md
workforce/changes/1.30.2.md
workforce/changes/1.30.3.md
workforce/changes/1.31.0.md
workforce/changes/1.32.0.md
workforce/changes/1.32.1.md
workforce/changes/1.32.2.md
workforce/changes/1.32.3.md
workforce/changes/1.32.4.md
# ── end change record ──
