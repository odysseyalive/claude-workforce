#!/usr/bin/env bash
# Maintainer-only. Wired by bin/dev-hooks-install (sets core.hooksPath -> githooks/).
# Delegates to the tested Python logic so the rule lives in one place.
exec python3 "$(git rev-parse --show-toplevel)/bin/pre-commit-version"
