# Rules

<!-- origin: user | immutable: true -->
> **"Provenance lines are never reflowed."**
<!-- /origin -->

Documentation about the format: write `<!-- origin: user | immutable: true -->`,
then the text, then `<!-- /origin -->`. Two mentions in that sentence.

<!-- origin: user | immutable: true -->
> **"A generated block is never hand-edited without a dated note."**
<!-- /origin -->

One more mention for good measure: `<!-- origin: quarrygen | modifiable: true -->`.
