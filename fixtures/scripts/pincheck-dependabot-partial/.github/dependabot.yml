# hand written by the repo owner; the guard must never edit or reorder these lines
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    open-pull-requests-limit: 3
    schedule:
      interval: "monthly"
