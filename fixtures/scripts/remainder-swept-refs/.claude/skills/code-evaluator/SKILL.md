---
name: code-evaluator
description: Evaluate code.
---
# Code Evaluator

Wire the gate with `/skill-builder code-eval enforce` before the first review.
The skill-builder origin marker is provenance and is not a reference.
`/skill-builder-extra` is a different command and is not the swept one.
