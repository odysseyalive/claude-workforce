# Remaining work - fixture

Every row below is the one category `references/deferred.md` allows to survive: a fix that lies in
another repository.

| # | Finding | What discharges it |
|---|---|---|
| ../../../../../../tmp/wf-upstream-escape | a row id shaped like a traversal | An upstream fix. |
| /etc/shadow | an absolute row id | An upstream fix. |
| a b$(whoami);rm -rf | shell metacharacters in a row id | An upstream fix. |
