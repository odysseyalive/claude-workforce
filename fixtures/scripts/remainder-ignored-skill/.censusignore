# The declared corpus exclusion under test. `overlay` is a foreign tree vendored
# from another repository — it is not this project's content, so the dead-script
# scan must not read it at all.
.claude/skills/overlay
