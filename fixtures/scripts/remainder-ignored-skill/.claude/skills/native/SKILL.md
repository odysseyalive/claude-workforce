---
name: native
description: "Fixture skill. Project content, holding one genuine dead reference."
---

# Native

## Scripts

Run `scripts/native-gone.sh` to finish. Nothing by that name exists in this tree, so
it is UNRESOLVED.

This skill is NOT declared out, so the exclusion above must not reach it. An
exclusion that blankets the whole scan is indistinguishable from deleting the
detector, and it would read as a clean tree.
