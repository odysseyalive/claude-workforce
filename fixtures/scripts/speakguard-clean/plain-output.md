# Plain Output

## Questions specifically

**Banned from question text and from every option label** — checked, not advisory: `sweep`,
`orchestrator`, `IC`, `Lead`, `tier`, `canary`, `handbook`, `employee`, `statics`, `pool`, `lane`,
`pre-selected`.

*Frozen fixture copy. The shipped list is longer; this one is deliberately short so the fixture
proves the PARSE and the match, not the shipped list's contents.*
