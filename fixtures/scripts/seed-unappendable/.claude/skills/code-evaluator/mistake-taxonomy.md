<!-- origin: user | immutable: true -->
# Mistake taxonomy — hand-authored by the user, immutable to end of file

Every byte of this catalog sits inside the user's immutable span; there is no
position outside it at which a machine-owned region could open.
