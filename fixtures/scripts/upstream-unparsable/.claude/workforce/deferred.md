# Remaining work - fixture

There is no table here at all. A prior run wrote its residue as prose, which is the state
`references/deferred.md` § Location and shape forbids and real trees still reach.

- one row about an upstream parser fix
- another about the install
