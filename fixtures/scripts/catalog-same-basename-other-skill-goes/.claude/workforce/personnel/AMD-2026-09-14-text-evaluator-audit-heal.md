# AMD-2026-09-14-text-evaluator-audit-heal

**Status:** accepted

The audit healed `text-evaluator`'s catalog read. The old literal path was
`.claude/skills/image-eval/references/ai-patterns.md`; it now resolves through `wf-catalog`.
