# Org config (frozen fixture — a legacy instantiation that copied the statics in)

## Effort statics
A stale copy of the ladder, left here by an older instantiation. It names three rungs that are not the
template's five and recommends `low` for every lane. The emitter must read PAST it — never from it —
and must never rewrite or delete it: the project file is the user's.

The rungs are `high`, `medium`, `low`.

| Lane (canonical) | Recommended effort |
|---|---|
| `analytical · Lead` | `low` |
| `analytical · IC` | `low` |
| `creative-text` | `low` |
| `creative-visual` | `low` |
| `code` | `low` |

## The four lanes
### Analytical
| Tier | Model | Effort |
|---|---|---|
| Lead (2) | `claude-opus-4-8` | high |
| IC (3) | `claude-sonnet-5` | medium |

### Code
| Code model | `claude-opus-5` |
|---|---|
| Code effort | high |
