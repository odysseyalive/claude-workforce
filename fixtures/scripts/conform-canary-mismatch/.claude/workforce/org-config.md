# org-config — fixture

## Measured this run

| | |
|---|---|
| `INV-SPAWN` | `AVAILABLE` · 1 attempt · measured behaviourally |
| `INV-CANARY` | attempt 1 `PASS` (Step 4b) · C1 `A=has-agent B=has-agent C=no-agent` → TIER-LIMIT 3 · C2 `agent-withheld` |
| harness | `claude --version` → **2.1.223** |
