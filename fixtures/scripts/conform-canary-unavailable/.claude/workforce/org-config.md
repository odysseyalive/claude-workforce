# org-config — fixture

## Measured this run

| | |
|---|---|
| `INV-CANARY` | attempt 1 `UNAVAILABLE` (Step 4b, fixtures unregistered) · attempt 2 `UNAVAILABLE` (Step 6a) |
| harness | `claude --version` → **2.1.223** |
