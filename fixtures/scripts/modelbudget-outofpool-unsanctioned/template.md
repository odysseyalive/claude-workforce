# org-config template (frozen fixture — a SECOND out-of-pool recommendation, which is refused)

## Model statics — the pool the model budget proposes from

`claude-fable-5` — the frontier visual model — is **Recommended for creative-visual** (graphics & frontend design work). It is deliberately NOT a pool row, and it is the ONE sanctioned out-of-pool recommendation.

THE DEFECT THIS FIXTURE FREEZES — `claude-haiku-4-5` is **Recommended for code** on a prose line, and the `claude-opus-5` row below has lost its annotation. The code lane therefore resolves to a model that is not in the pool. Nothing in the emission could distinguish that from the pool having gone stale, which is the drift the script exists to end, so it must be refused rather than rendered.

| # | Model ID | Context | Max output | Notes |
|---|---|---|---|---|
| 1 | `claude-opus-5` | 1M | 128K | strongest at programming; commits and drives |
| 2 | `claude-opus-4-8` | 1M | 128K | the steerable thought-partner. **Recommended for analytical Lead** (agents that coordinate) |
| 3 | `claude-opus-4-6` | 1M | 128K | **Recommended for creative-text** (writing & copy work) |
| 4 | `claude-sonnet-5` | 1M | 128K | near-Opus quality at ~40% lower cost. **Recommended for analytical IC** (agents that do the work) |

## Effort statics

placeholder — not parsed by wf-model-budget.
