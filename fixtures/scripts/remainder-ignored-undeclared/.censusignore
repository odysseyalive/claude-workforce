# The SAME tree as remainder-ignored-skill, with a .censusignore that declares
# something ELSE. The exclusion is DECLARED, never inferred from a directory name —
# so both skills are project content here and both dead references must report.
testdata/
