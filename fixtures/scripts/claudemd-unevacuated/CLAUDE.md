# Project

Deploy only from the release branch after the smoke suite passes.
Every migration is reviewed by two people before it runs against production.
Never run the destructive migration script against the production database directly.
