---
name: loose-layout
description: A design skill parked outside any skills/ container, on purpose.
---

# loose-layout

The plugin layout this project has not measured. It must be reported by path, never
silently skipped — a matcher that drops what it does not recognise reports a clean
sweep over the skills it never looked at.
