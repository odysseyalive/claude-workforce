---
name: demo
---
# demo

## Commands
| Command | Action |
|---|---|
| `/demo run` | run it |
| `/demo sync` | sync it |

Invoke `scripts/gather.sh` then `bin/report.py`.

## When to use
Use it when you feel the urge. Prefer it over guessing.

## Schema
| col | type |

## Connection
Server: alpaca. Read verbs: `get_account`, `list_orders`.
