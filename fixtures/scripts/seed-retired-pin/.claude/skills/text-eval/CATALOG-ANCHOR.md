# Reconciliation anchor — text-eval (fixture)

<!-- text-eval-ref-version: 4 -->

<!-- additions-seeded: tag=text version=7 method=retired retired=2026-09-25 -->

`wf-catalog --migrate` retired this copy: it removed `references/text-tells.md`, the file
the old `region-file=` pin named, and left `references/ai-patterns.md` and
`references/colon-detection.md` standing because live readers cite them. The directory is
therefore NOT empty, which is the only thing wf-seed used to test. The pin above is the
record that makes the retirement readable.
